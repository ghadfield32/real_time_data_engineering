packages:
  # dbt-utils: cross-database macros (surrogate_key, date_spine, pivot, etc.)
  # Provides: dbt_utils.generate_surrogate_key(), dbt_utils.date_spine(),
  #           dbt_utils.unpivot(), dbt_utils.get_column_values(), and more
  # Used in: marts models for surrogate key generation and date dimension spine
  - package: dbt-labs/dbt_utils
    version: ">=1.1.0"
