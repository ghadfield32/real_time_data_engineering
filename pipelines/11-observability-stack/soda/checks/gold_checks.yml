# Analytics layer validation — checks dbt-materialized marts and dimensions
checks for mart_daily_revenue:
  - row_count > 0:
      name: "Daily revenue mart has data"
  - min(total_revenue) > 0:
      name: "All days have positive revenue"
  - min(total_trips) > 0:
      name: "All days have trips"

checks for dim_locations:
  - row_count > 200:
      name: "Location dimension is populated"
  - duplicate_count(location_id) = 0:
      name: "No duplicate location IDs"
